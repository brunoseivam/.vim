Mostly stolen from @jessfraz:

https://github.com/jessfraz/.vim

    $ cd ~/
    $ git clone --recursive https://github.com/brunoseivam/.vim.git .vim
    $ ln -sf $HOME/.vim/vimrc $HOME/.vimrc
    $ cd $HOME/.vim
    $ git submodule update --init



